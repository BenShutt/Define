# AppIcon

Swift package library with functionality that should be shared with the main app and AppIconGenerator.

# AppIconGenerator

Swift package executable that generates an AppIcon and LaunchScreen image from SwiftUI views. 
